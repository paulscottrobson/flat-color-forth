System:

	Convert back to compilation rule compiles

		Red : add definition, add CALL <compile self>
		Green : call if in dictionary, numeric constant
		Yellow : Exec in place (macro or std compilation) or do number switch.									


- add string constant compiler only.
- if/-if crashout ?
- cross page call
- port libraries.


